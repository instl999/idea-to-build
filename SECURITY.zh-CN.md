# 安全策略

[English](SECURITY.md) | **简体中文**

## 支持版本

安全修复计划仅面向最新发布版本。2026-08-06 本地与 `origin` 可确认的最近 Git tag 是 `v0.2.0`；0.4.0 manifest 分支尚无 tag，其发布/支持状态须在公开发布前确认。

## 报告漏洞

不要在公开 Issue 中提交利用细节、令牌、个人数据或尚未披露的漏洞。请使用仓库 GitHub 的 **Security → Report a vulnerability** 私有安全公告入口，说明受影响版本/commit、环境、复现步骤、影响和最小化且已脱敏的证明。如果私有入口不可用，只公开请求建立私密联系渠道，不要附带漏洞详情。

维护者应在七天内确认收到，完成验证和分级，协调修复与披露日期，并按报告者意愿署名。本项目不承诺漏洞奖金。

## 安全模型

插件不包含 MCP server、远程后端、认证或内置凭据。本地脚本拥有用户授予的文件系统与 Git 权限。实时研究由宿主 Web Search 完成，查询文字可能发送给宿主/搜索提供方。

冻结核心依赖精确 Git 根、SHA-256、锁文件、人工审查和 Git 历史；只读位与 Hooks 是辅助控制。Hooks 可以阻止或发现许多直接操作，但不能认证“人类身份”、撤销已执行修改，也看不到所有外部编辑器、进程或工具。生成提示词和研究输入都应按不可信数据处理。

## 生成产品的 MCP 安全

MCP 候选、安装说明、工具描述、资源和 server 输出都属于不可信输入。Skill 只在 readiness 后评估 MCP，绝不替用户安装 server、执行复制来的安装命令、写入凭据或修改宿主配置。

采用前必须核验当前官方来源、发布者/维护/许可、宿主与 transport 兼容性、认证与最小权限、数据去向、写操作工具、更新/移除路径和直接集成退路；使用合成数据覆盖正常、鉴权/错误/超时/server 不可用场景，高影响工具必须保留用户显式确认。

## 子智能体与工作树安全

生成的提示词和子智能体摘要都属于不可信输入。只有产品核心已由人类确认冻结，且用户在当前任务中要求 Codex 继续开发时，调度才会启动。适配器要求精确且干净的 Git 根、hash 有效的清单、已跟踪的提示词、直接同级工作树、互不重叠的所有权、预期分支头、正确祖先关系，以及非空且完全位于所有权内的差异。受验证的集成必须使用 `merge-result`；冲突会中止，子分支会保留。适配器拒绝移除脏工作树并保留分支用于恢复。这些控制不能替代代码审查、测试、宿主权限和 Git 备份。

## 维护者发布清单

- 审查完整 diff 和依赖面。
- 运行全部测试、编译、包校验、冻结示例校验和 `scripts/audit_public_release.py`。
- 确认所有 commit 作者/提交者邮箱只匹配获准形式：配置的 no-reply 后缀，或 GitHub 合成提交使用的精确 `noreply@github.com` 地址；相似域名仍拒绝。
- 宿主套餐支持时启用 GitHub secret scanning、push protection、默认分支保护和必需 CI。
- 不得为了发布通过而削弱失败的保护规则。

另见[隐私](docs/PRIVACY.zh-CN.md)、[架构](docs/ARCHITECTURE.zh-CN.md)与[变更控制](docs/CHANGE_CONTROL.zh-CN.md)。

## 仓库记忆与质量门禁安全

任务、规格和提示词文本都是不可信数据。路径必须是仓库相对路径，并拒绝穿越、链接、junction、受保护区域和并行所有权重叠。质量命令必须显式、无 shell、输出受限且绑定当前快照；未知/未来 schema、过期证据、未知门禁、不安全命令和非法转换全部失败关闭。人工验收、核心确认和冻结仍仅限人类。任务 JSON、质量记录、hash 和 Hook 都不是独立安全边界。
