# Start task

Render the complete localized prompt with:

`python scripts/memory_prompts.py show --path . --kind start-task --task TASK-0001`

It verifies core, SPEC, acceptance, dependencies, ownership, task transition, PLAN, and quality-gate expectations before code changes.
