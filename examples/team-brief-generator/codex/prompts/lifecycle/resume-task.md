# Resume task

`python scripts/memory_prompts.py show --path . --kind resume-task --task TASK-0001`

Resume only the same explicit task. Reconcile SPEC, PLAN, diff, task status, and quality snapshot; never guess a parallel task.
