# Worked example: Team Brief Generator

This directory is a complete generated Idea-to-Build project package. It demonstrates a confirmed 38-category requirements ledger, a human-approved SHA-256 core lock, 21 generated design documents, and an exact seven-thread Codex handoff.

The research candidates are explicitly fictional fixtures. Re-run live three-layer research before treating the build decision as current market evidence.

Validate from this directory:

```bash
python scripts/verify_core.py --path .
python scripts/validate_package.py
```

Start with `codex/HANDOFF.md`, then use the self-contained prompt matching each planned thread.
