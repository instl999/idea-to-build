# Core Change Requests

After core freeze, AI conversations may add proposals here but must never change frozen files or the lock.

Each request records: identifier, date, reason, affected constraints, alternatives, migration cost, risks, requested human decision, and status.