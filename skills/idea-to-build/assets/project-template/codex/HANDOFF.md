# Codex handoff

Not generated. Run `python scripts/generate_handoff.py --path .` after the core is frozen.

Generation also creates `codex/dispatch.json`. After freeze, a user request to proceed with development lets Codex choose the root agent or native subagent/worktree tools from the actual dependency graph.
