# Synchronize task state

`python scripts/memory_prompts.py show --path . --kind sync-tasks`

Validate the canonical JSON ledger and regenerate its Markdown projection. Chat is not task-state evidence.
