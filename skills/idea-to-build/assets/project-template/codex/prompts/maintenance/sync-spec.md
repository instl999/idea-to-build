# Synchronize one task specification

`python scripts/memory_prompts.py show --path . --kind sync-spec --task TASK-0001`

Strengthen scope, evidence labels, dependencies, failures, security/privacy, gates, open questions, and observable acceptance.
