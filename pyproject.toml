[project]
name = "idea-to-build-codex-plugin"
version = "0.1.0"
description = "Tests and tooling for the Idea-to-Build Codex plugin"
requires-python = ">=3.9"
license = { text = "MIT" }

[tool.idea-to-build]
schema-version = 1
test-command = "python -m unittest discover -s tests -v"